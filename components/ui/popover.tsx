'use client'
import * as React from 'react'

// Minimal stub to avoid @radix-ui/react-popover dependency.
// Keeps existing imports working without pulling any package.

export const Popover = ({ children }: { children: React.ReactNode }) => <>{children}</>
export const PopoverTrigger = ({ children }: { children: React.ReactNode }) => <>{children}</>
export const PopoverContent = ({ children }: { children: React.ReactNode }) => <>{children}</>
